from distutils.core import setup

setup(
    name='SkipgramSampler',
    version='0.1dev',
    packages=['SkipgramSampler'],
    license='MIT',
    long_description='A library that allows for the sampling of skipgrams from a corpus using the techniques of Mikolov et al. 2013'
)